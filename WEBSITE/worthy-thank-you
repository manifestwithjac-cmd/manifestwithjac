<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Your Worthy Activation — Manifest With Jac</title>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;1,300;1,400&family=Outfit:wght@300;400;500&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

```
:root {
  --rose: #e784ba;
  --violet: #c5a8e1;
  --mint: #a8d8c8;
  --dark: #080810;
}

body {
  background-color: var(--dark);
  color: #fff;
  font-family: 'Outfit', sans-serif;
  min-height: 100vh;
  overflow-x: hidden;
}

canvas.stars {
  position: fixed;
  inset: 0;
  z-index: 0;
  pointer-events: none;
}

.orb {
  position: fixed;
  border-radius: 50%;
  filter: blur(80px);
  pointer-events: none;
  z-index: 0;
}
.orb-1 { width: 500px; height: 500px; background: var(--rose); top: -100px; right: -100px; opacity: 0.15; }
.orb-2 { width: 400px; height: 400px; background: var(--violet); bottom: -80px; left: -80px; opacity: 0.12; }

.page {
  position: relative;
  z-index: 1;
  max-width: 700px;
  margin: 0 auto;
  padding: 60px 24px 80px;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.eyebrow {
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.3em;
  text-transform: uppercase;
  color: var(--rose);
  margin-bottom: 20px;
  opacity: 0;
  animation: fadeUp 0.8s 0.2s forwards;
}

h1 {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(38px, 7vw, 60px);
  font-weight: 300;
  line-height: 1.1;
  margin-bottom: 16px;
  opacity: 0;
  animation: fadeUp 0.8s 0.4s forwards;
}

h1 em {
  font-style: italic;
  background: linear-gradient(135deg, var(--rose), var(--violet));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.subtitle {
  font-size: 15px;
  font-weight: 300;
  color: rgba(255,255,255,0.55);
  max-width: 480px;
  line-height: 1.7;
  margin-bottom: 40px;
  opacity: 0;
  animation: fadeUp 0.8s 0.6s forwards;
}

/* Video */
.video-wrap {
  width: 100%;
  max-width: 660px;
  border-radius: 16px;
  overflow: hidden;
  border: 1px solid rgba(255,255,255,0.08);
  box-shadow: 0 0 80px rgba(231,132,186,0.12), 0 0 0 1px rgba(197,168,225,0.1);
  aspect-ratio: 16/9;
  opacity: 0;
  animation: fadeUp 0.8s 0.8s forwards;
  position: relative;
}

.video-wrap iframe {
  width: 100%;
  height: 100%;
  border: none;
  display: block;
}

.divider {
  width: 60px;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--rose), transparent);
  margin: 48px auto;
  opacity: 0;
  animation: fadeUp 0.8s 1s forwards;
}

.message {
  max-width: 500px;
  opacity: 0;
  animation: fadeUp 0.8s 1.1s forwards;
}

.message p {
  font-size: 15px;
  font-weight: 300;
  color: rgba(255,255,255,0.6);
  line-height: 1.8;
  margin-bottom: 12px;
}

.message p strong {
  color: rgba(255,255,255,0.9);
  font-weight: 400;
}

.sig {
  margin-top: 24px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 22px;
  font-style: italic;
  background: linear-gradient(135deg, var(--rose), var(--violet));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

@keyframes fadeUp {
  from { opacity: 0; transform: translateY(20px); }
  to   { opacity: 1; transform: translateY(0); }
}

@media (max-width: 480px) {
  .page { padding: 40px 20px 60px; }
}
```

  </style>
</head>
<body>

<canvas class="stars" id="stars"></canvas>

<div class="orb orb-1"></div>
<div class="orb orb-2"></div>

<div class="page">

  <p class="eyebrow">She's been here all along</p>

  <h1>Welcome home,<br><em>gorgeous.</em></h1>

  <p class="subtitle">
    This is a 10-minute guided pineal gland activation. Find somewhere quiet, put your headphones in, and let yourself actually receive this.
  </p>

  <div class="video-wrap">
    <iframe
      src="https://www.youtube.com/embed/LXEn-X99MZk?rel=0&modestbranding=1"
      title="The Worthy Activation"
      allowfullscreen
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture">
    </iframe>
  </div>

  <div class="divider"></div>

  <div class="message">
    <p>
      <strong>You are already worthy.</strong> Not after he comes back. Not after something changes. Right now.
    </p>
    <p>
      This activation will drop you into theta brainwaves and rewire your subconscious to finally feel safe receiving — love, money, all of it. Same work I do with private clients. Let it land.
    </p>
    <p>
      Optional: hand over heart during the anchor moment. Comment "WORTHY" on the video if you felt the shift. 💌
    </p>
    <div class="sig">— Jac</div>
  </div>

</div>

<script>
  const canvas = document.getElementById('stars');
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;

  const stars = Array.from({length: 120}, () => ({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height,
    r: Math.random() * 1.2 + 0.2,
    speed: Math.random() * 0.008 + 0.003,
    phase: Math.random() * Math.PI * 2
  }));

  function drawStars(t) {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    stars.forEach(s => {
      const opacity = 0.15 + 0.55 * (0.5 + 0.5 * Math.sin(s.phase + t * s.speed));
      ctx.beginPath();
      ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(255,255,255,${opacity})`;
      ctx.fill();
    });
    requestAnimationFrame(drawStars);
  }
  requestAnimationFrame(drawStars);

  window.addEventListener('resize', () => {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
  });
</script>

</body>
</html>